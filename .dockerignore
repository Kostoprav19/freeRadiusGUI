# VCS / IDE
.git
.gitignore
.idea
*.iml
.vscode
.cursor

# Build output
target/
*.class
*.war
*.jar

# Legacy IDEA web module layout (Maven uses src/main/webapp/)
web/

# Local dev/test stack & sample / reference data (not needed in the image)
lab/

# Docs & local tooling
README.md
AGENTS.md
*.md
.mise.toml
mise.toml
.mise.local.toml

# Compose / env (not needed inside the build context — the whole lab/
# tree is already excluded above, this catches any stragglers at repo root)
compose.yaml
compose.yml
docker-compose.yaml
docker-compose.yml
.env
.env.example

# OS junk
.DS_Store
Thumbs.db
