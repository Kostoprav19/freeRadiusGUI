#java specific
*.class

# Local environment overrides
.env
lab/.env
.mise.local.toml

# Maven build output
target/

# Tool-specific dirs
.cursor/**
.cursor/rules/
.cursor/rules/**
.claude/
.codex/
# Production deploy overrides (operator-local; .example siblings tracked)
deploy/.env
deploy/config.properties

# Operator/dev-supplied RADIUS config, bind-mounted into freeradius + app.
# Holds real shared secrets and is rewritten by the app on "Apply changes".
# (deploy/radius-config/ is kept via its own .gitignore; lab/ is fully ignored.)
lab/radius-config/
