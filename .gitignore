#java specific
*.class

# Local environment overrides
.env
lab/.env
.mise.local.toml

# Maven build output
target/

# Tool-specific dirs — canonical subagent prompts live in agents/ at
# repo root; .cursor/agents, .claude/agents, .codex/agents are local
# symlinks pointing at it (plus tool-private state we don't track).
.cursor/
.claude/
.codex/
