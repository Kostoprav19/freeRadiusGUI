# Dev defaults for the lab stack. The smoke test (and `mise run compose:up`)
# copy this into lab/radius-config/ (git-ignored), which is bind-mounted into
# the freeradius + app containers. Safe placeholder secrets only - never put
# real NAS secrets here.

client dockernet {
    ipaddr = 172.16.0.0/12
    secret = testing123
    shortname = dockernet
}

client localhost {
    ipaddr = 127.0.0.1
    secret = testing123
    shortname = localhost
}
