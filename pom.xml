<?xml version="1.0" encoding="UTF-8" ?>
<project
  xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
>
    <modelVersion>4.0.0</modelVersion>

    <groupId>lv.freeradiusgui</groupId>
    <artifactId>freeRadiusGui</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>war</packaging>

    <properties>
        <!--
          Runtime: JDK 17 + Tomcat 9. Namespace: javax.* (not jakarta.*).
          Framework versions are pinned to the last javax line of each:
            - Spring 5.3.x is the last javax line; 6.x is jakarta + JDK 17.
            - Spring Security 5.8.x mirrors that.
            - Hibernate 5.6.x ships both javax (hibernate-core) and jakarta
              (hibernate-core-jakarta) artifacts; we stay on javax.
            - Thymeleaf 3.1.x keeps javax support (3.2+ dropped it).
          Do not bump any of these past the pinned majors without also moving
          to jakarta and Tomcat 10 — they travel together in Phase 4.
        -->
        <spring-version>5.3.39</spring-version>
        <spring-security-version>5.8.15</spring-security-version>
        <junit-version>4.12</junit-version>
        <javax-api-version>3.1.0</javax-api-version>
        <hibernate-version>5.6.15.Final</hibernate-version>
        <mysql-connector-version>8.4.0</mysql-connector-version>
        <!-- HikariCP 4.x is the last javax-aligned line; 5.x ships jakarta.sql coords. -->
        <hikaricp-version>4.0.3</hikaricp-version>
        <thymeleaf-version>3.1.4.RELEASE</thymeleaf-version>
        <thymeleaf-security>3.1.3.RELEASE</thymeleaf-security>
        <slf4j-version>1.7.21</slf4j-version>
        <!-- logback 1.2.x is EOL upstream but compatible with our pinned slf4j 1.7.x;
             1.3.x needs slf4j 2.x (separate phase). 1.2.13 includes CVE-2021-42550 fix. -->
        <logback-version>1.2.13</logback-version>
        <javax.mail-version>1.5.6</javax.mail-version>
        <!-- javax.annotation.PostConstruct was in JDK <= 8; removed in JDK 11
             by JEP 320. Provide it explicitly as an artifact to keep our javax.* stack
             compiling on JDK 17. 1.3.2 is the last javax-namespace release. -->
        <javax.annotation-version>1.3.2</javax.annotation-version>
    </properties>

    <dependencies>


        <!--LOGGING-->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j-version}</version>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback-version}</version>
        </dependency>

        <!--CONNECTION POOL-->
        <dependency>
            <groupId>com.zaxxer</groupId>
            <artifactId>HikariCP</artifactId>
            <version>${hikaricp-version}</version>
        </dependency>

        <!--MYSQL-->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>${mysql-connector-version}</version>
        </dependency>

        <!--TEST-->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>${junit-version}</version>
            <scope>test</scope>
        </dependency>

        <!--SERVLET API-->
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>javax.servlet-api</artifactId>
            <version>${javax-api-version}</version>
            <scope>provided</scope>
        </dependency>

        <!--javax.annotation (@PostConstruct) — removed from JDK 11+ (JEP 320)-->
        <dependency>
            <groupId>javax.annotation</groupId>
            <artifactId>javax.annotation-api</artifactId>
            <version>${javax.annotation-version}</version>
        </dependency>

        <!--SPRING-->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webmvc</artifactId>
            <version>${spring-version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-tx</artifactId>
            <version>${spring-version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-orm</artifactId>
            <version>${spring-version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
            <version>${spring-version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-test</artifactId>
            <version>${spring-version}</version>
            <scope>test</scope>
        </dependency>

        <!-- SPRING SECURITY-->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-core</artifactId>
            <version>${spring-security-version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-web</artifactId>
            <version>${spring-security-version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-config</artifactId>
            <version>${spring-security-version}</version>
        </dependency>

        <!--THYMELEAF-->
        <dependency>
            <groupId>org.thymeleaf</groupId>
            <artifactId>thymeleaf</artifactId>
            <version>${thymeleaf-version}</version>
        </dependency>

        <dependency>
            <groupId>org.thymeleaf</groupId>
            <artifactId>thymeleaf-spring5</artifactId>
            <version>${thymeleaf-version}</version>
        </dependency>

        <dependency>
            <groupId>org.thymeleaf.extras</groupId>
            <artifactId>thymeleaf-extras-springsecurity5</artifactId>
            <version>${thymeleaf-security}</version>
            <scope>compile</scope>
        </dependency>

        <!--HIBERNATE-->
        <dependency>
            <groupId>org.hibernate</groupId>
            <artifactId>hibernate-core</artifactId>
            <version>${hibernate-version}</version>
        </dependency>

        <!--MAIL-->
        <dependency>
            <groupId>com.sun.mail</groupId>
            <artifactId>javax.mail</artifactId>
            <version>${javax.mail-version}</version>
        </dependency>

    </dependencies>

    <build>
        <finalName>freeradiusgui</finalName>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>17</release>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.4.0</version>
            </plugin>

            <!--LINT / FORMAT-->
            <!--
              Spotless — runs on explicit invocation only, not bound to a phase,
              so `mvn package` / `mvn test` / the Docker build are unaffected.
                Check:   mvn spotless:check    (mise run lint)
                Apply:   mvn spotless:apply    (mise run format)
              Version ceilings (tied to our JDK 17 runtime, per Phase 3):
                - spotless-maven-plugin 2.44.5 (2.31.0+ requires JRE 11+; fine on 17)
                - google-java-format   1.26.0 (1.25–1.26 require JDK 17; 1.27+ needs JDK 21)
            -->
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>2.44.5</version>
                <configuration>
                    <java>
                        <includes>
                            <include>src/main/java/**/*.java</include>
                            <include>src/test/java/**/*.java</include>
                        </includes>
                        <googleJavaFormat>
                            <version>1.26.0</version>
                            <style>AOSP</style>
                        </googleJavaFormat>
                        <removeUnusedImports />
                        <trimTrailingWhitespace />
                        <endWithNewline />
                    </java>
                </configuration>
            </plugin>

            <!--TESTS-->
            <!--
              argLine opens java.base packages that Hibernate 5.6 + Spring 5.3
              reflect into. Required under JDK 17 (strong encapsulation); without
              these, DAO tests throw InaccessibleObjectException at first
              sessionFactory.openSession(). Mirrored in the Dockerfile JAVA_OPTS.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <argLine>
                        --add-opens=java.base/java.lang=ALL-UNNAMED
                        --add-opens=java.base/java.lang.reflect=ALL-UNNAMED
                        --add-opens=java.base/java.lang.invoke=ALL-UNNAMED
                        --add-opens=java.base/java.util=ALL-UNNAMED
                        --add-opens=java.base/java.math=ALL-UNNAMED
                    </argLine>
                    <includes>
                        <include>**/*IT.java</include>
                        <include>**/*TestIT.java</include>
                        <include>**/*Test.java</include>
                    </includes>
                </configuration>
            </plugin>

        </plugins>
    </build>

</project>
