<?xml version="1.0" encoding="UTF-8" ?>
<project
  xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
>
    <modelVersion>4.0.0</modelVersion>

    <groupId>lv.freeradiusgui</groupId>
    <artifactId>freeRadiusGui</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>war</packaging>

    <properties>
        <!--
          Runtime: JDK 17 + Tomcat 10.1. Namespace: jakarta.* (not javax.*).
          Framework versions sit on the current OSS-supported lines after the
          Phase 4 jakarta crossing:
            - Spring 6.1.x — JDK 17 minimum, jakarta.* only. 6.0 is EOL,
              6.2 has additional breaking changes; 6.1 lines up with the
              spring-security 6.3.x and Spring Boot 3.3.x line.
            - Spring Security 6.3.x — pairs with Spring 6.1.
            - Hibernate stays at 5.6.15.Final but on the *jakarta* artifact
              (hibernate-core-jakarta). Same bytecode as the javax artifact
              with persistence imports rewritten — keeps our custom
              EnhancedUserType (CustomLocalDateTime) unchanged. Hibernate 6
              breaks that interface again and is deferred to Phase 5.
            - Thymeleaf 3.1.x — same core version as Phase 3, but the Spring
              integration artifacts are now -spring6 / -springsecurity6.
            - HikariCP 5.x — current jakarta-aligned line.
            - jakarta.mail 2.0.2 — first 2.0.x with the SMTP command-injection
              fix (2.0.1 has a published advisory).
          Do not bump Hibernate past 5.6.15.Final without the Phase 5 plan
          landed — Hibernate 6 changes Criteria/EnhancedUserType/dialect APIs.
        -->
        <spring-version>6.1.14</spring-version>
        <spring-security-version>6.3.4</spring-security-version>
        <junit-version>4.12</junit-version>
        <!-- Servlet 6.0 = Tomcat 10.1 / Jakarta EE 10 baseline. Spring 6.1
             supports 5.0+ but at runtime we deploy on Tomcat 10.1 which
             ships 6.0 (jakarta.servlet.ServletConnection lives there);
             pinning the API at 6.0 keeps compile/test in lockstep with the
             runtime. Tomcat 11 / Servlet 6.1 requires JDK 21 (Phase 8). -->
        <jakarta-servlet-api-version>6.0.0</jakarta-servlet-api-version>
        <hibernate-version>5.6.15.Final</hibernate-version>
        <mysql-connector-version>8.4.0</mysql-connector-version>
        <hikaricp-version>5.1.0</hikaricp-version>
        <thymeleaf-version>3.1.4.RELEASE</thymeleaf-version>
        <thymeleaf-security>3.1.3.RELEASE</thymeleaf-security>
        <slf4j-version>1.7.21</slf4j-version>
        <!-- logback 1.2.x is EOL upstream but still compatible with our pinned
             slf4j 1.7.x; the slf4j 2 / logback 1.5 bump is Phase 6. 1.2.13
             includes the CVE-2021-42550 fix. -->
        <logback-version>1.2.13</logback-version>
        <jakarta.mail-version>2.0.2</jakarta.mail-version>
        <!-- jakarta.annotation.PostConstruct: still needed as an explicit dep
             on JDK 17 (JEP 320 removed javax.annotation from the JDK in 11;
             jakarta.annotation lives entirely outside the JDK). -->
        <jakarta.annotation-version>2.1.1</jakarta.annotation-version>
    </properties>

    <dependencies>


        <!--LOGGING-->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j-version}</version>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback-version}</version>
        </dependency>

        <!--CONNECTION POOL-->
        <dependency>
            <groupId>com.zaxxer</groupId>
            <artifactId>HikariCP</artifactId>
            <version>${hikaricp-version}</version>
        </dependency>

        <!--MYSQL-->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>${mysql-connector-version}</version>
        </dependency>

        <!--TEST-->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>${junit-version}</version>
            <scope>test</scope>
        </dependency>

        <!--SERVLET API-->
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <version>${jakarta-servlet-api-version}</version>
            <scope>provided</scope>
        </dependency>

        <!--jakarta.annotation (@PostConstruct) — not in the JDK since JEP 320 (Java 11)-->
        <dependency>
            <groupId>jakarta.annotation</groupId>
            <artifactId>jakarta.annotation-api</artifactId>
            <version>${jakarta.annotation-version}</version>
        </dependency>

        <!--SPRING-->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webmvc</artifactId>
            <version>${spring-version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-tx</artifactId>
            <version>${spring-version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-orm</artifactId>
            <version>${spring-version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
            <version>${spring-version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-test</artifactId>
            <version>${spring-version}</version>
            <scope>test</scope>
        </dependency>

        <!-- SPRING SECURITY-->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-core</artifactId>
            <version>${spring-security-version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-web</artifactId>
            <version>${spring-security-version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-config</artifactId>
            <version>${spring-security-version}</version>
        </dependency>

        <!--THYMELEAF-->
        <dependency>
            <groupId>org.thymeleaf</groupId>
            <artifactId>thymeleaf</artifactId>
            <version>${thymeleaf-version}</version>
        </dependency>

        <dependency>
            <groupId>org.thymeleaf</groupId>
            <artifactId>thymeleaf-spring6</artifactId>
            <version>${thymeleaf-version}</version>
        </dependency>

        <dependency>
            <groupId>org.thymeleaf.extras</groupId>
            <artifactId>thymeleaf-extras-springsecurity6</artifactId>
            <version>${thymeleaf-security}</version>
            <scope>compile</scope>
        </dependency>

        <!--HIBERNATE-->
        <!-- hibernate-core-jakarta = same 5.6 bytecode with javax.persistence
             rewritten to jakarta.persistence. Kept on 5.6.x; Hibernate 6 is
             Phase 5. -->
        <dependency>
            <groupId>org.hibernate</groupId>
            <artifactId>hibernate-core-jakarta</artifactId>
            <version>${hibernate-version}</version>
        </dependency>

        <!--MAIL-->
        <dependency>
            <groupId>com.sun.mail</groupId>
            <artifactId>jakarta.mail</artifactId>
            <version>${jakarta.mail-version}</version>
        </dependency>

    </dependencies>

    <build>
        <finalName>freeradiusgui</finalName>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>17</release>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.4.0</version>
            </plugin>

            <!--LINT / FORMAT-->
            <!--
              Spotless — runs on explicit invocation only, not bound to a phase,
              so `mvn package` / `mvn test` / the Docker build are unaffected.
                Check:   mvn spotless:check    (mise run lint)
                Apply:   mvn spotless:apply    (mise run format)
              Version ceilings (tied to our JDK 17 runtime, per Phase 3):
                - spotless-maven-plugin 2.44.5 (2.31.0+ requires JRE 11+; fine on 17)
                - google-java-format   1.26.0 (1.25–1.26 require JDK 17; 1.27+ needs JDK 21)
            -->
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>2.44.5</version>
                <configuration>
                    <java>
                        <includes>
                            <include>src/main/java/**/*.java</include>
                            <include>src/test/java/**/*.java</include>
                        </includes>
                        <googleJavaFormat>
                            <version>1.26.0</version>
                            <style>AOSP</style>
                        </googleJavaFormat>
                        <removeUnusedImports />
                        <trimTrailingWhitespace />
                        <endWithNewline />
                    </java>
                </configuration>
            </plugin>

            <!--TESTS-->
            <!--
              argLine opens java.base packages that Hibernate 5.6 + Spring 6.1
              reflect into. Required under JDK 17 (strong encapsulation); without
              these, DAO tests throw InaccessibleObjectException at first
              sessionFactory.openSession(). Mirrored in the Dockerfile JAVA_OPTS.
              Phase 3 included a fifth open (java.base/java.math) preemptively;
              Phase 4 dropped it after verifying tests pass without it. Phase 5
              (Hibernate 6 + MethodHandles) is expected to drop most of these.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <argLine>
                        --add-opens=java.base/java.lang=ALL-UNNAMED
                        --add-opens=java.base/java.lang.reflect=ALL-UNNAMED
                        --add-opens=java.base/java.lang.invoke=ALL-UNNAMED
                        --add-opens=java.base/java.util=ALL-UNNAMED
                    </argLine>
                    <includes>
                        <include>**/*IT.java</include>
                        <include>**/*TestIT.java</include>
                        <include>**/*Test.java</include>
                    </includes>
                </configuration>
            </plugin>

        </plugins>
    </build>

</project>
